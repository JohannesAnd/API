extends ./../MainMenu

block header
    script(type="text/javascript").
        var id = !{org.id};
    script(src = "/js/carOverview.js", type = "text/javascript")


block content
    .orgContainer
        a(href="/organizations/#{org.id}") < Back to #{org.name}
        #map_canvas
        br
        h2="Car Overview for "+org.name
        #active
        #inactive
